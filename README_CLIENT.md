# 📦 Инструкция по установке - Краткая версия для клиентов

## ⚡ Установка за 3 шага

### 1️⃣ Распакуйте архив и запустите установку

```bash
unzip eg-web-installer-1.0.0.zip
cd eg-web-installer-1.0.0
chmod +x install.sh
sudo ./install.sh
```

**Введите:**
- Домен (или `localhost`)
- Пароль БД (или Enter для автогенерации)
- Email
- Веб-сервер (`nginx` или `apache`)

### 2️⃣ Активируйте лицензию

```bash
curl -X POST http://ваш-домен/api/license/activate \
  -H "Content-Type: application/json" \
  -d '{"license_key": "ВАШ-КЛЮЧ-ЛИЦЕНЗИИ"}'
```

### 3️⃣ Откройте сайт

```
http://ваш-домен
```

**Готово!** Создайте первого пользователя через регистрацию.

---

## 📋 Что установится автоматически

- PHP 8.3, MySQL, Nginx/Apache
- SSL сертификат (если указан домен)
- Все зависимости и настройки

## 🔄 Обновление

```bash
cd /var/www/eg-web
sudo ./update.sh
```

## ❓ Проблемы?

**Логи:**
- Установка: `/var/log/eg-web-install.log`
- Приложение: `/var/www/eg-web/storage/logs/laravel.log`

**Поддержка:** [ваш email]

---

**Подробная инструкция:** см. `INSTALLATION_CLIENT.md`
