# Как работает система лицензирования

## 🏗️ Архитектура системы

Система состоит из нескольких компонентов, которые работают вместе:

```
┌─────────────────┐
│   .env файл     │ ← Хранит ключ лицензии (LICENSE_KEY)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ LicenseService  │ ← Центральный сервис для работы с лицензиями
└────────┬────────┘
         │
         ├─────────────────┬─────────────────┐
         ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│   License    │  │   License    │  │  Middleware  │
│   Model      │  │  Controller  │  │              │
└──────────────┘  └──────────────┘  └──────────────┘
         │
         ▼
┌─────────────────┐
│ База данных     │
│ - licenses      │
│ - license_      │
│   features      │
└─────────────────┘
```

## 🔄 Процесс работы

### 1. Активация лицензии

Когда клиент покупает проект и получает ключ лицензии:

```
Клиент → POST /api/license/activate
         { "license_key": "PREMIUM-XXXX-XXXX-XXXX" }
         
         ↓
         
LicenseService::activate()
  ├─ Ищет лицензию в БД по ключу
  ├─ Проверяет валидность (не истекла ли)
  ├─ Проверяет домен (если привязан)
  └─ Сохраняет ключ в .env файл
```

**Результат:** Ключ сохраняется в `.env` как `LICENSE_KEY=PREMIUM-XXXX-XXXX-XXXX`

### 2. Проверка лицензии при запросах

При каждом запросе к защищенным маршрутам:

```
Запрос → Middleware CheckLicense
           ↓
           LicenseService::getCurrentLicense()
             ├─ Читает LICENSE_KEY из config('license.key')
             ├─ Ищет лицензию в БД (с кешированием)
             └─ Возвращает объект License
           ↓
           LicenseService::checkLicense()
             ├─ Проверяет is_active = true
             ├─ Проверяет expires_at (не истекла ли)
             └─ Проверяет домен (если привязан)
           ↓
           ✅ Разрешить запрос ИЛИ ❌ Вернуть 403
```

### 3. Проверка функций

Когда нужно проверить, доступна ли конкретная функция:

```
has_license_feature('api_access')
  ↓
LicenseService::hasFeature('api_access')
  ↓
License::hasFeature('api_access')
  ├─ Проверяет isValid() (лицензия действительна?)
  ├─ Ищет в таблице license_features
  │   WHERE feature_key = 'api_access' AND enabled = true
  └─ ИЛИ проверяет в JSON поле features
  ↓
✅ true (доступна) ИЛИ ❌ false (недоступна)
```

## 📊 Структура данных

### Таблица `licenses`

```php
[
    'license_key' => 'PREMIUM-XXXX-XXXX-XXXX',  // Уникальный ключ
    'type' => 'premium',                        // Тип лицензии
    'domain' => 'client-site.com',             // Привязка к домену (опционально)
    'expires_at' => '2025-12-31',              // Дата истечения
    'is_active' => true,                        // Активна ли
    'max_users' => 100,                         // Лимит пользователей
    'max_requests_per_month' => 100000,        // Лимит запросов
    'features' => ['api_access', 'export_data'], // Функции (JSON)
    'customer_email' => 'client@example.com',
]
```

### Таблица `license_features`

```php
[
    'license_id' => 1,
    'feature_key' => 'api_access',
    'enabled' => true,
    'settings' => ['rate_limit' => 1000]  // Доп. настройки (JSON)
]
```

## 🎯 Примеры работы

### Пример 1: Защита маршрута

```php
// routes/api.php
Route::middleware(['auth:api', 'license:api_access'])->group(function () {
    Route::get('/api/data', [DataController::class, 'index']);
});
```

**Что происходит:**
1. Пользователь делает запрос: `GET /api/data`
2. Middleware `license:api_access` перехватывает запрос
3. Проверяет лицензию → ✅ Действительна
4. Проверяет функцию `api_access` → ✅ Доступна
5. Проверяет лимит пользователей → ✅ Не превышен
6. Разрешает запрос → Контроллер выполняется

### Пример 2: Условное отображение в коде

```php
// В контроллере
public function exportData()
{
    if (!has_license_feature('export_data')) {
        return response()->json([
            'message' => 'Экспорт недоступен в вашей лицензии'
        ], 403);
    }
    
    // Выполнить экспорт
    return $this->performExport();
}
```

**Что происходит:**
1. Вызывается `has_license_feature('export_data')`
2. Система проверяет текущую лицензию
3. Ищет функцию `export_data` в license_features
4. Если найдена и enabled=true → возвращает true
5. Если нет → возвращает false → возвращается ошибка 403

### Пример 3: Проверка в Vue компоненте

```javascript
// В Vue компоненте
async mounted() {
    const response = await api.get('/license/feature/advanced_reports');
    if (response.data.available) {
        this.showAdvancedReports = true;
    }
}
```

**Что происходит:**
1. Vue компонент делает запрос к API
2. `LicenseController::checkFeature()` получает запрос
3. Вызывает `LicenseService::hasFeature('advanced_reports')`
4. Возвращает `{ available: true/false }`
5. Vue компонент показывает/скрывает функцию

## 🔐 Безопасность

### Защита от обхода:

1. **Проверка домена:**
   - Лицензия может быть привязана к конкретному домену
   - При проверке сравнивается текущий домен с привязанным

2. **Проверка срока действия:**
   - Каждая лицензия имеет `expires_at`
   - Система автоматически блокирует истекшие лицензии

3. **Проверка активности:**
   - Лицензию можно деактивировать (`is_active = false`)
   - Это позволяет отозвать лицензию удаленно

4. **Кеширование:**
   - Результаты проверки кешируются на 1 час
   - Уменьшает нагрузку на БД, но не влияет на безопасность

## 📈 Лимиты

### Лимит пользователей

```php
// При регистрации нового пользователя
if (!$licenseService->checkUserLimit()) {
    return error('Достигнут лимит пользователей');
}
```

**Как работает:**
- Считает текущее количество пользователей в БД
- Сравнивает с `max_users` из лицензии
- Если превышен → блокирует регистрацию

### Лимит запросов

```php
// В middleware автоматически увеличивается счетчик
$licenseService->incrementRequestCount();
```

**Как работает:**
- Каждый запрос увеличивает счетчик в кеше
- Счетчик сбрасывается в конце месяца
- Если превышен `max_requests_per_month` → блокирует запросы

## 🎨 Типы лицензий

### Trial (Пробная)
- 30 дней
- 1 пользователь
- Только `basic_auth`

### Basic (Базовая)
- 1 год
- 10 пользователей
- `basic_auth` + `user_management`

### Premium (Премиум)
- 1 год
- 100 пользователей
- Все функции кроме enterprise

### Enterprise (Корпоративная)
- Без ограничений по времени
- Без ограничений пользователей
- Все функции включены

## 🔄 Жизненный цикл лицензии

```
1. Создание лицензии
   ↓
2. Выдача клиенту (ключ)
   ↓
3. Активация клиентом
   ↓
4. Проверка при каждом запросе
   ↓
5. Истечение срока / Деактивация
   ↓
6. Блокировка доступа
```

## 💡 Практические советы

### Где использовать проверки:

1. **В маршрутах** - для защиты целых разделов
2. **В контроллерах** - для условной логики
3. **В Blade шаблонах** - для скрытия элементов UI
4. **В Vue компонентах** - для динамического интерфейса

### Оптимизация:

- Используйте кеширование (уже реализовано)
- Проверяйте лицензию один раз за запрос
- Используйте middleware для массовой защиты маршрутов

## ❓ Частые вопросы

**Q: Что если лицензия не активирована?**
A: `getCurrentLicense()` вернет `null`, все проверки вернут `false`

**Q: Можно ли использовать одну лицензию на нескольких доменах?**
A: Нет, если указан `domain`, лицензия привязана к одному домену

**Q: Как отозвать лицензию?**
A: Установите `is_active = false` в БД, лицензия перестанет работать

**Q: Что если истек срок лицензии?**
A: `isValid()` вернет `false`, все проверки будут блокировать доступ

**Q: Как добавить новую функцию?**
A: Добавьте в `config/license.php` в массив `features`, затем создайте записи в `license_features`
