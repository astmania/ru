# 📦 Создание установочного пакета

## Вариант 1: Полный пакет (с кодом проекта)

Создает архив со всем проектом и установочными скриптами:

```bash
chmod +x build-package.sh
./build-package.sh
```

Результат: `build/eg-web-installer-1.0.0.zip`

## Вариант 2: Минимальный пакет (только скрипты)

Создает архив только с установочными скриптами (без кода проекта):

```bash
chmod +x build-package-minimal.sh
./build-package-minimal.sh
```

Результат: `build/eg-web-installer-minimal-1.0.0.zip`

## 📋 Что включено в пакет

### Полный пакет:
- ✅ Весь код проекта
- ✅ Установочные скрипты
- ✅ Документация
- ✅ Готов к установке "из коробки"

### Минимальный пакет:
- ✅ Только установочные скрипты
- ✅ Документация
- ⚠️ Требует отдельного получения кода проекта

## 🚀 Использование пакета

### Для клиента (полный пакет):

1. Распакуйте архив:
   ```bash
   unzip eg-web-installer-1.0.0.zip
   cd eg-web-installer-1.0.0
   ```

2. Запустите установку:
   ```bash
   chmod +x install.sh check-requirements.sh update.sh
   sudo ./install.sh
   ```

### Для клиента (минимальный пакет):

1. Получите код проекта (Git, архив и т.д.)
2. Распакуйте минимальный пакет
3. Скопируйте скрипты в директорию проекта
4. Запустите установку

## 📝 Рекомендации

- **Для продажи:** Используйте полный пакет - клиент получает всё готовое
- **Для разработки:** Используйте минимальный пакет + Git репозиторий
- **Для обновлений:** Используйте `update.sh` скрипт

## 🔒 Безопасность

Перед распространением пакета:
- [ ] Убедитесь, что нет чувствительных данных в коде
- [ ] Проверьте, что `.env` файл не включен
- [ ] Убедитесь, что нет паролей и ключей в коде
- [ ] Проверьте права доступа к файлам
