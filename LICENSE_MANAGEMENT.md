# Управление лицензиями через веб-интерфейс

## 📋 Обзор

После установки проекта вы можете управлять лицензиями через веб-интерфейс. Система предоставляет полный функционал для создания, редактирования, активации и деактивации лицензий.

## 🔐 Доступ к управлению лицензиями

### Требования:

1. **Авторизация** - необходимо быть авторизованным пользователем
2. **Права администратора** - пользователь должен иметь `is_admin = true`

### Создание администратора:

```bash
# Через tinker
php artisan tinker

# В консоли:
$user = App\Models\User::where('email', 'admin@example.com')->first();
$user->is_admin = true;
$user->save();
```

Или через миграцию/сидер:

```php
User::create([
    'name' => 'Admin',
    'email' => 'admin@example.com',
    'password' => Hash::make('password'),
    'is_admin' => true,
]);
```

## 🌐 Доступ к интерфейсу

После авторизации администратором откройте:
```
http://ваш-домен/licenses
```

## 🎯 Возможности интерфейса

### 1. Просмотр всех лицензий
- Список всех лицензий с пагинацией
- Фильтрация по типу, статусу
- Поиск по ключу, email, имени клиента

### 2. Статистика
- Всего лицензий
- Активных лицензий
- Истекших лицензий
- Неактивных лицензий

### 3. Создание лицензии
- Выбор типа (trial, basic, premium, enterprise)
- Автогенерация ключа или ручной ввод
- Настройка домена
- Установка даты истечения
- Настройка лимитов (пользователи, запросы)
- Информация о клиенте

### 4. Редактирование лицензии
- Изменение всех параметров
- Обновление функций лицензии
- Изменение статуса

### 5. Управление статусом
- Активация/деактивация лицензии
- Мгновенное применение изменений

### 6. Удаление лицензии
- Удаление с подтверждением
- Защита от удаления текущей активной лицензии

## 📡 API Endpoints

### Публичные (для клиентов):

```bash
# Активация лицензии
POST /api/license/activate
{
    "license_key": "XXXX-XXXX-XXXX-XXXX",
    "domain": "example.com"
}

# Проверка лицензии
GET /api/license/check

# Информация о лицензии
GET /api/license/info

# Проверка функции
GET /api/license/feature/{feature}
```

### Административные (требуют авторизации и прав админа):

```bash
# Список лицензий
GET /api/license/list?page=1&per_page=20&search=...&type=...&status=...

# Статистика
GET /api/license/statistics

# Создание лицензии
POST /api/license/create
{
    "type": "premium",
    "domain": "example.com",
    "expires_at": "2025-12-31",
    "max_users": 100,
    ...
}

# Получить лицензию
GET /api/license/{id}

# Обновить лицензию
PUT /api/license/{id}
{
    "type": "enterprise",
    "is_active": true,
    ...
}

# Удалить лицензию
DELETE /api/license/{id}

# Изменить статус
POST /api/license/{id}/toggle-status

# Обновить функции
PUT /api/license/{id}/features
{
    "features": [
        {
            "feature_key": "api_access",
            "enabled": true,
            "settings": {}
        }
    ]
}
```

## 🔧 Использование через API

### Пример создания лицензии через curl:

```bash
curl -X POST http://your-domain.com/api/license/create \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "premium",
    "domain": "client-site.com",
    "expires_at": "2025-12-31",
    "max_users": 100,
    "customer_email": "client@example.com",
    "customer_name": "Иван Иванов"
  }'
```

### Пример получения списка лицензий:

```bash
curl -X GET "http://your-domain.com/api/license/list?page=1&per_page=20&type=premium" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 🎨 Интерфейс управления

### Основные элементы:

1. **Статистика** - карточки с общей информацией
2. **Фильтры** - поиск и фильтрация по типу/статусу
3. **Таблица лицензий** - список всех лицензий
4. **Модальное окно** - создание/редактирование
5. **Действия** - кнопки для управления каждой лицензией

### Функции интерфейса:

- ✅ Копирование ключа лицензии в буфер обмена
- ✅ Быстрое переключение статуса
- ✅ Редактирование всех параметров
- ✅ Удаление с подтверждением
- ✅ Пагинация для больших списков

## 🔄 Процесс работы

### Создание лицензии для клиента:

1. Войдите как администратор
2. Перейдите в раздел "Управление лицензиями"
3. Нажмите "Создать лицензию"
4. Заполните форму:
   - Тип лицензии
   - Домен клиента (опционально)
   - Дата истечения
   - Лимиты
   - Информация о клиенте
5. Сохраните
6. Скопируйте ключ лицензии и передайте клиенту

### Активация лицензии клиентом:

1. Клиент получает ключ лицензии
2. При установке проекта вызывает:
   ```bash
   POST /api/license/activate
   {
       "license_key": "XXXX-XXXX-XXXX-XXXX"
   }
   ```
3. Лицензия активируется и сохраняется в `.env`

### Управление лицензией:

- **Деактивация** - мгновенно блокирует доступ
- **Изменение лимитов** - обновляет ограничения
- **Продление** - изменение даты истечения
- **Удаление** - полное удаление из системы

## 🔐 Безопасность

1. **Проверка прав** - только администраторы могут управлять лицензиями
2. **Защита текущей лицензии** - нельзя удалить активную лицензию
3. **Валидация данных** - все данные проверяются перед сохранением
4. **Логирование** - все действия логируются

## 📝 Примеры использования

### Создание trial лицензии:

```json
{
    "type": "trial",
    "expires_at": "2025-02-24",
    "max_users": 1,
    "customer_email": "trial@example.com"
}
```

### Создание premium лицензии:

```json
{
    "type": "premium",
    "domain": "client-site.com",
    "expires_at": "2025-12-31",
    "max_users": 100,
    "max_requests_per_month": 100000,
    "customer_email": "client@example.com",
    "customer_name": "ООО Компания"
}
```

### Обновление лицензии:

```json
{
    "max_users": 200,
    "expires_at": "2026-12-31",
    "is_active": true
}
```

## 🐛 Решение проблем

### Проблема: Нет доступа к управлению лицензиями

**Решение:**
1. Проверьте, что пользователь имеет `is_admin = true`
2. Проверьте авторизацию (токен валиден)
3. Проверьте middleware в маршрутах

### Проблема: Ошибка при создании лицензии

**Решение:**
1. Проверьте валидацию данных
2. Проверьте логи: `storage/logs/laravel.log`
3. Убедитесь, что база данных доступна

### Проблема: Лицензия не активируется

**Решение:**
1. Проверьте, что лицензия существует в БД
2. Проверьте, что лицензия активна и не истекла
3. Проверьте домен (если привязан)

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `storage/logs/laravel.log`
2. Проверьте права доступа пользователя
3. Проверьте статус сервисов

---

**Версия:** 1.0.0  
**Дата:** 2026-01-24
