# 📦 Инструкция по установке для клиентов

## 🎯 Что вы получили

После покупки лицензии вы получили:
- ✅ Установочный пакет проекта (ZIP архив)
- ✅ Ключ лицензии (например: `PREMIUM-XXXX-XXXX-XXXX`)
- ✅ Инструкцию по установке (этот файл)

## ⚡ Быстрая установка (3 простых шага)

### Шаг 1: Загрузите и распакуйте архив

```bash
# Загрузите архив на сервер (через FTP, SCP или другим способом)
# Распакуйте архив
unzip eg-web-installer-1.0.0.zip
cd eg-web-installer-1.0.0
```

### Шаг 2: Запустите автоматическую установку

```bash
# Сделайте скрипты исполняемыми
chmod +x install.sh check-requirements.sh

# Проверьте требования (опционально)
sudo ./check-requirements.sh

# Запустите установку
sudo ./install.sh
```

**Во время установки введите:**
- **Домен:** ваш домен (например: `mysite.com`) или `localhost` для тестирования
- **Пароль БД:** пароль для базы данных (или нажмите Enter для автогенерации)
- **Email:** ваш email (для SSL сертификата)
- **Веб-сервер:** выберите `nginx` (рекомендуется) или `apache`

**Установка займет 5-15 минут.** Скрипт автоматически установит все необходимое.

### Шаг 3: Активируйте лицензию

После установки активируйте вашу лицензию:

```bash
curl -X POST http://ваш-домен/api/license/activate \
  -H "Content-Type: application/json" \
  -d '{
    "license_key": "ВАШ-КЛЮЧ-ЛИЦЕНЗИИ",
    "domain": "ваш-домен.com"
  }'
```

**Пример:**
```bash
curl -X POST http://mysite.com/api/license/activate \
  -H "Content-Type: application/json" \
  -d '{
    "license_key": "PREMIUM-1234-5678-ABCD",
    "domain": "mysite.com"
  }'
```

**Готово!** Откройте `http://ваш-домен` в браузере и создайте первого пользователя.

---

## 📋 Подробная инструкция

### Требования к серверу

- **ОС:** Ubuntu 20.04 LTS или выше
- **RAM:** Минимум 1GB (рекомендуется 2GB+)
- **Диск:** Минимум 2GB свободного места
- **Интернет:** Требуется для установки зависимостей
- **Права:** root (sudo)

### Процесс установки

1. **Загрузите архив на сервер**
   - Через SSH: `scp eg-web-installer-1.0.0.zip user@server:/tmp/`
   - Через FTP/SFTP: загрузите файл на сервер

2. **Подключитесь к серверу по SSH**
   ```bash
   ssh user@your-server.com
   ```

3. **Распакуйте архив**
   ```bash
   cd /tmp  # или куда загрузили
   unzip eg-web-installer-1.0.0.zip
   cd eg-web-installer-1.0.0
   ```

4. **Проверьте требования (рекомендуется)**
   ```bash
   chmod +x check-requirements.sh
   sudo ./check-requirements.sh
   ```

5. **Запустите установку**
   ```bash
   chmod +x install.sh
   sudo ./install.sh
   ```

6. **Следуйте инструкциям на экране**

### Что установится автоматически

- ✅ PHP 8.3 с необходимыми расширениями
- ✅ Composer (менеджер зависимостей PHP)
- ✅ Node.js 20.x и npm
- ✅ MySQL 8.0 база данных
- ✅ Nginx или Apache веб-сервер
- ✅ SSL сертификат (Let's Encrypt, если указан домен)
- ✅ Все зависимости проекта
- ✅ Настроены права доступа
- ✅ Настроены cron задачи

### Активация лицензии

После установки обязательно активируйте лицензию:

```bash
curl -X POST http://ваш-домен/api/license/activate \
  -H "Content-Type: application/json" \
  -d '{
    "license_key": "ВАШ-КЛЮЧ-ЛИЦЕНЗИИ",
    "domain": "ваш-домен.com"
  }'
```

**Успешный ответ:**
```json
{
  "success": true,
  "message": "Лицензия успешно активирована",
  "license": { ... }
}
```

### Проверка работы

1. **Откройте сайт:**
   ```
   http://ваш-домен
   ```
   или (если использован домен)
   ```
   https://ваш-домен
   ```

2. **Проверьте API:**
   ```bash
   curl http://ваш-домен/api/health
   ```

3. **Создайте первого пользователя:**
   - Откройте сайт в браузере
   - Нажмите "Регистрация"
   - Заполните форму

## 🔄 Обновление проекта

Для обновления проекта:

```bash
cd /var/www/eg-web
sudo ./update.sh
```

Скрипт автоматически:
- Создаст резервную копию БД и файлов
- Обновит код и зависимости
- Выполнит миграции
- Перезапустит сервисы

## 🔐 Безопасность после установки

### 1. Настройте firewall

```bash
sudo ufw allow 22/tcp   # SSH
sudo ufw allow 80/tcp   # HTTP
sudo ufw allow 443/tcp  # HTTPS
sudo ufw enable
```

### 2. Измените пароли

- **Пароль root MySQL:** сохранен в `/root/.mysql_root_password`
- **Пароли пользователей:** измените через интерфейс приложения

### 3. Настройте резервные копии

```bash
# Добавьте в crontab (crontab -e)
0 2 * * * mysqldump -u root -pПАРОЛЬ eg_web > /backup/db_$(date +\%Y\%m\%d).sql
```

## ❓ Решение проблем

### Проблема: Ошибка при установке

**Решение:**
1. Проверьте логи: `tail -f /var/log/eg-web-install.log`
2. Убедитесь, что Ubuntu 20.04+
3. Проверьте интернет соединение
4. Убедитесь, что достаточно места (минимум 2GB)

### Проблема: Сайт не открывается

**Решение:**
```bash
# Проверьте статус сервисов
sudo systemctl status php8.3-fpm
sudo systemctl status nginx
sudo systemctl status mysql

# Перезапустите при необходимости
sudo systemctl restart php8.3-fpm
sudo systemctl restart nginx
```

### Проблема: Лицензия не активируется

**Решение:**
1. Проверьте правильность ключа лицензии
2. Убедитесь, что домен совпадает (если лицензия привязана к домену)
3. Проверьте логи: `tail -f /var/www/eg-web/storage/logs/laravel.log`

### Проблема: Ошибка прав доступа

**Решение:**
```bash
sudo chown -R www-data:www-data /var/www/eg-web
sudo chmod -R 755 /var/www/eg-web
sudo chmod -R 775 /var/www/eg-web/storage
sudo chmod -R 775 /var/www/eg-web/bootstrap/cache
```

## 📍 Важная информация

**Директория проекта:** `/var/www/eg-web`

**Файл конфигурации:** `/var/www/eg-web/.env`

**Логи:**
- Установка: `/var/log/eg-web-install.log`
- Приложение: `/var/www/eg-web/storage/logs/laravel.log`

**Пароль root MySQL:** `/root/.mysql_root_password`

## 📞 Поддержка

При возникновении проблем обратитесь в службу поддержки:

**Email:** [ваш email поддержки]

**Укажите:**
- Домен проекта
- Ключ лицензии
- Описание проблемы
- Логи ошибок (если есть)

**Где найти логи:**
```bash
# Логи установки
tail -f /var/log/eg-web-install.log

# Логи приложения
tail -f /var/www/eg-web/storage/logs/laravel.log

# Логи веб-сервера
tail -f /var/log/nginx/error.log
```

## ✅ Чеклист после установки

- [ ] Проект доступен по адресу `http://ваш-домен`
- [ ] SSL сертификат установлен (если использован домен)
- [ ] Лицензия активирована
- [ ] Можно зарегистрироваться и войти
- [ ] Все функции работают
- [ ] Firewall настроен
- [ ] Пароли изменены на безопасные
- [ ] Резервные копии настроены

---

**Версия:** 1.0  
**Дата:** 2026-01-24

**Готово!** Ваш проект установлен и готов к работе.

Если возникли вопросы, обратитесь в службу поддержки.
