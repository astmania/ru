# Система мониторинга и управления развернутыми проектами

## 📋 Обзор

Система позволяет управлять всеми развернутыми проектами из одного места (управляющий проект), проверять их работоспособность и получать доступ для устранения неисправностей.

## 🎯 Возможности

- ✅ **Регистрация проектов** - автоматическая регистрация развернутых проектов
- ✅ **Мониторинг здоровья** - автоматическая проверка работоспособности
- ✅ **Управление проектами** - просмотр, редактирование, удаление
- ✅ **Статистика** - общая статистика по всем проектам
- ✅ **Уведомления** - уведомления о проблемах
- ✅ **Удаленный доступ** - информация для SSH доступа

## 🔐 Права доступа

### Супер-администратор (`is_super_admin = true`)
- Может управлять развернутыми проектами
- Может просматривать статистику
- Может проверять здоровье проектов
- Может редактировать/удалять проекты

### Обычный администратор (`is_admin = true`)
- Может управлять лицензиями
- Не может управлять развернутыми проектами

## 🚀 Регистрация развернутого проекта

### Автоматическая регистрация

После установки проекта запустите скрипт регистрации:

```bash
# На развернутом проекте
chmod +x register-project.sh
./register-project.sh
```

Или вручную через переменные окружения:

```bash
export MANAGEMENT_URL="http://your-management-server.com"
export PROJECT_NAME="Проект клиента"
export PROJECT_URL="http://client-project.com"
export LICENSE_KEY="XXXX-XXXX-XXXX-XXXX"

./register-project.sh
```

### Ручная регистрация через API

```bash
curl -X POST http://management-server.com/api/projects/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Проект клиента",
    "url": "http://client-project.com",
    "license_key": "XXXX-XXXX-XXXX-XXXX",
    "api_key": "dp_...",
    "api_secret": "..."
  }'
```

## 🔍 Проверка работоспособности

### Автоматическая проверка

Система автоматически проверяет все проекты каждые 5 минут через cron:

```php
// В routes/console.php уже настроено
Schedule::command('projects:check-health')->everyFiveMinutes();
```

### Ручная проверка

```bash
# Проверка всех проектов
php artisan projects:check-health

# Проверка конкретного проекта
php artisan projects:check-health --project-id=1
```

### Через API

```bash
# Проверка одного проекта
POST /api/projects/{id}/check-health

# Проверка всех проектов
POST /api/projects/check-all-health
```

## 📊 Что проверяется

1. **Доступность URL** - проверка HTTP ответа
2. **API Health Endpoint** - проверка `/api/health`
3. **Лицензия** - проверка валидности лицензии
4. **Время ответа** - измерение производительности

## 🌐 Health Endpoint для развернутых проектов

Развернутые проекты должны иметь endpoint `/api/health`:

```php
// В routes/api.php развернутого проекта
Route::get('/health', function () {
    return response()->json([
        'status' => 'ok',
        'timestamp' => now()->toIso8601String(),
        'version' => config('app.version', '1.0.0'),
    ]);
});
```

## 🎨 Веб-интерфейс управления

### Доступ

Только супер-администраторы могут видеть раздел "Проекты" в меню.

### Функции интерфейса:

- 📊 Статистика всех проектов
- 📋 Список всех развернутых проектов
- ➕ Добавление нового проекта
- ✏️ Редактирование проекта
- 🔍 Проверка здоровья (ручная)
- 🗑️ Удаление проекта
- 📧 Информация о контактах

## 📡 API Endpoints

### Публичные:

```bash
# Регистрация проекта (вызывается самим проектом)
POST /api/projects/register
{
    "name": "Проект",
    "url": "http://project.com",
    "license_key": "XXXX-XXXX-XXXX-XXXX",
    "api_key": "dp_...",
    "api_secret": "..."
}

# Health check (для мониторинга)
GET /api/health
```

### Супер-администраторские:

```bash
# Список проектов
GET /api/projects?page=1&per_page=20&search=...&status=...

# Статистика
GET /api/projects/statistics

# Создание проекта
POST /api/projects
{
    "name": "Проект",
    "url": "http://project.com",
    "license_key": "XXXX-XXXX-XXXX-XXXX",
    "server_ip": "192.168.1.1",
    "server_user": "root",
    "contact_email": "client@example.com"
}

# Получить проект
GET /api/projects/{id}

# Обновить проект
PUT /api/projects/{id}

# Удалить проект
DELETE /api/projects/{id}

# Проверить здоровье
POST /api/projects/{id}/check-health

# Проверить все проекты
POST /api/projects/check-all-health
```

## 🔧 Настройка автоматической проверки

### В управляющем проекте:

1. Настройте cron для Laravel:
```bash
* * * * * cd /path-to-project && php artisan schedule:run >> /dev/null 2>&1
```

2. Команда уже настроена в `routes/console.php`:
```php
Schedule::command('projects:check-health')->everyFiveMinutes();
```

## 🐛 Устранение неисправностей

### Доступ к развернутому проекту:

1. **Через SSH** (если указаны данные):
   - `server_ip` - IP сервера
   - `server_user` - пользователь
   - `ssh_key` - SSH ключ (если есть)

2. **Через API**:
   - Используйте `api_key` и `api_secret` для доступа к API проекта

### Логи и диагностика:

- Проверьте статус проекта в интерфейсе
- Просмотрите результаты последней проверки здоровья
- Проверьте логи развернутого проекта

## 📝 Примеры использования

### Создание супер-администратора:

```bash
php artisan tinker
```

```php
$user = App\Models\User::where('email', 'superadmin@example.com')->first();
$user->is_super_admin = true;
$user->save();
```

### Регистрация проекта после установки:

```bash
# На развернутом проекте
export MANAGEMENT_URL="http://management.example.com"
export LICENSE_KEY="PREMIUM-XXXX-XXXX-XXXX"
./register-project.sh
```

### Проверка здоровья через API:

```bash
curl -X POST http://management.example.com/api/projects/1/check-health \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 🔔 Уведомления

Система может отправлять уведомления при обнаружении проблем:

- Email уведомления (если настроено)
- Webhook уведомления (если настроено)
- Логирование в базу данных

## 📊 Статистика

Интерфейс показывает:
- Всего проектов
- Здоровых проектов
- Проектов с ошибками
- Проектов на обслуживании
- Неактивных проектов

## 🔐 Безопасность

1. **API ключи** - каждый проект имеет уникальные ключи
2. **Проверка прав** - только супер-админы могут управлять проектами
3. **Шифрование** - SSH ключи хранятся в зашифрованном виде (рекомендуется)
4. **Логирование** - все действия логируются

## 📞 Поддержка

При проблемах:
1. Проверьте статус проекта в интерфейсе
2. Выполните ручную проверку здоровья
3. Проверьте логи: `storage/logs/laravel.log`
4. Проверьте доступность проекта по URL

---

**Версия:** 1.0.0  
**Дата:** 2026-01-24
